name: eo
channels:
  - conda-forge
dependencies: 
  - pip
  - python>=3.8             # Paseos core dependency
  - numpy==1.23.5           # Paseos core dependency
  - dotmap>=1.3.30          # Paseos core dependency
  - loguru>=0.6.0           # Paseos core dependency
  - myst-parser             # For markdown math in docs
  - pykep>=2.6              # Paseos core dependency
  - pyquaternion>=0.9.9     # Paseos core dependency
  - pytest<8.0.0            # Paseos core dependency
  - pytest-asyncio          # For tests. v8.0.0 is not supporting asyncio.
  - scikit-spatial>=6.5.0   # Paseos core dependency
  - skyfield>=1.45          # Paseos core dependency
  - sphinx                  # For docs
  - sphinx_rtd_theme        # For docs  
  - toml>=0.10.2            # Paseos core dependency
  - tqdm>=4.64.1            # Paseos core dependency
  - trimesh>=4.0.7          # For geometric model
  - matplotlib>=3.6.0       # Paseos core dependency
  - pip:
    - gitpython             # For cloning repo